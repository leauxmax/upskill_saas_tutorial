<div class="home-callout container-fluid">
  <div class="text-center container">
    <!--<h1 style="font-size:365%;">Welcome to Jig</h1>-->
    <h1 style="font-size:805%;">💬✨💬<h1>
  </div>
</div>
<div class="container fluid", style="color: #ff00cc">
  <div class="row">
    <% if user_signed_in? %>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Your Profile</h2>
          <% if current_user.profile %>
            <%= link_to "Edit your profile", edit_user_profile_path(user_id: current_user.id), class: 'btn btn-default btn-large btn-block' %>
            <%= link_to "Peep your profile", user_path(id: current_user.id), class: 'btn btn-default btn-large btn-block' %>
          <% else %>
            <%= link_to "Make a jigga!", new_user_profile_path(user_id: current_user.id), class: 'btn btn-default btn-large btn-block' %>
          <% end %>
        </div>
      </div>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">The Community</h2>
          <%= link_to "Visit the community", users_path, class: 'btn btn-default btn-large btn-block' %>
        </div>
      </div>
      
    <% else %>
    <div class="col-md-4 col-md-offset-4">
      
    <!--  <div class="well">-->
    <!--    <h3 class="text-center", style="margin-top: -8px">Basic, for nuthin!🍕</h3>-->
        <%= link_to "✔", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-large btn-block' %> 
    <!--  </div>-->
    <!--</div>-->
    <!--<div class="col-md-6">-->
    <!--  <div class="well">-->
    <!--    <h3 class="text-center", style="margin-top: -8px">Treat. Yo. Self! $9/mo💸</h3>-->
    <!--    <%= link_to "Geaux Preaux!", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-large btn-block' %>-->
    <!--  </div>-->
    <!--</div>-->
    <% end %>
  </div>
  
</div> 

